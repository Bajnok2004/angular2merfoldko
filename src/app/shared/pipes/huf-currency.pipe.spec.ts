import { HufCurrencyPipe } from './huf-currency.pipe';

describe('HufCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new HufCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
